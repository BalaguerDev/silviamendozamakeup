---
import './ServiceCard.css'

const { titleCard, imageSrc, price } = Astro.props;
---

<div class="card">
    <img src={imageSrc} alt={titleCard} transition:name={titleCard}/>
    <div class="card-content">
        <h3>{titleCard}</h3>
        <p>{price}</p>
    </div>
</div>
